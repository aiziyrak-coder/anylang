# Admin panel (Next.js)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Production checklist:
# - Set NEXT_PUBLIC_API_URL to your public API origin (https://api.example.com)
# - Add the admin origin to backend CORS_ORIGINS (e.g. https://admin.example.com)
# - Serve admin behind HTTPS; JWT is stored in HttpOnly cookie via /api/auth/login
